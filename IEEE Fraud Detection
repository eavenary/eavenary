library(tidyverse)
library(randomForest)
library(tictoc)

tic()
train_iden <- read.csv("train_identity.csv")
train_trans <- read.csv("train_transaction.csv")
test_iden <- read.csv("test_identity.csv")
test_trans <- read.csv("test_transaction.csv")
submission <- read.csv("sample_submission.csv")
toc()

head(train_trans, n = 5)
head(train_iden, n = 5)
